{
  "name": "pi-way-skill",
  "version": "3.0.0",
  "description": "The pi philosophy — less is more, Unix-way. Sub-agents via Agent tool, Makefile orchestration, file-based planning. Injects pi-way principles into system prompt via before_agent_start hook.",
  "keywords": ["pi-package", "pi-way", "skills", "sub-agents", "workflow"],
  "author": "e4779",
  "license": "ISC",
  "scripts": {
    "postinstall": "rm -f \"$HOME/.pi/agent/APPEND_SYSTEM.md\" 2>/dev/null; echo 'pi-way: principles injected via extension (before_agent_start hook)' || true"
  },
  "pi": {
    "skills": ["./skills"],
    "extensions": ["./extensions/pi-way.ts"]
  }
}
